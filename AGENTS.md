# Gong 專案工作規則

## 每個新對話的開始
- 工作根目錄固定為 `C:\Users\User\Documents\Codex\Gong`，不要另建日期資料夾。
- 在提出實作建議或修改前，先讀取根目錄的 `PROJECT_STATE.md`、`DECISIONS.md`、`README.md`，不需要使用者提醒。
- 核對與本次任務相關的實際檔案及 Git 狀態。紀錄中的歷史驗證不代表現在已重新驗證。
- 紀錄不足、矛盾或需要原話時，使用可用的對話工具查找同專案相關對話，逐頁讀取到所需背景完整。不要把最近一個對話當成唯一來源。
- 如果紀錄不存在或工具無法存取舊對話，明確說明缺漏；先做不依賴缺漏的工作，不假裝知道歷史。
- 讀取背景是為了理解本次要求；未完成事項不代表可以自行恢復舊任務。

## 溝通與創作
- 使用繁體中文與清楚、非技術性的說明；必要時才補充技術細節。
- 個人表達、文案邊界、照片順序及已確認的選擇以 `DECISIONS.md` 為依據，避免反覆詢問。
- 不自行推測情緒原因、補寫不確定的回憶，或改變使用者對作品的定位。
- 不加入廣告、平台推廣、標誌或多餘樣板裝飾。保留必要授權與部署設定。
- 原始製作計畫已獲確認，不必為每次例行修正重問。新的重大創作方向先整理成可審查內容，再依使用者指示處理。
- 只在缺少必要資訊或超出已授權範圍時詢問；既有授權不重問。

## 私人資料與發布
- `PROJECT_STATE.md`、`DECISIONS.md` 是本機私人交接資料，維持 Git 忽略，不加入公開 GitHub、部署封裝或公開網站。
- 不把私人故事、原始照片、明文密碼、解密金鑰或存取憑證放進公開檔案、提交訊息或日誌。
- 密碼與金鑰由後端秘密設定提供；不要複製秘密到交接紀錄。
- 公開入口在 GitHub Pages，私人內容與驗證在既有後端；不得以純前端密碼檢查取代後端保護。
- 修改公開畫面時檢查 `docs/`；修改後端或私人內容時依 `README.md` 處理另一套建置與發布。
- 涉及網站實作或發布時，讀取適用技能；本機文件更新不等於要求發布網站。

## 持續交接
- 每次有重要進展、決定、失敗或範圍變更，及時更新 `PROJECT_STATE.md`；結束實作回合前完成更新。
- 使用者確認的新選擇記入 `DECISIONS.md`，附日期、依據與取代關係。提案、事實與已確認決定分開標示。
- 狀態記錄目前目標、完成事項、待辦、阻礙、驗證結果、下一步及相關對話 ID。
- 多個任務同時存在時分開記錄，更新前重讀檔案，不覆蓋其他任務的進度。
- 保持紀錄精簡，保留重要創作邊界與來源；舊決定被取代時標記，不讓它繼續指導工作。
- 執行與改動相稱的檢查；報告區分本次檢查、歷史結果和未驗證事項。單純文件變更不需要重跑整個網站測試。
